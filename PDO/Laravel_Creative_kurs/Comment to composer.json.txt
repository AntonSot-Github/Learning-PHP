{
    "autoload": {
        // Раздел "autoload" отвечает за автоматическую загрузку классов.
        "psr-4": {
            // Используется стандарт PSR-4 для организации пространств имен.
            "App\\": "app/"
            // Ключ "App\\" определяет пространство имен "App".
            // Значение "app/" указывает на каталог, где находятся файлы этого пространства имен.
            // Это значит, что классы с пространством имен "App" будут искаться в папке "app/".
        }
    }
}